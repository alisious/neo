<UserControl x:Class="Kseo2.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             
             Height="600" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Background="Yellow" Orientation="Horizontal">
            <Button x:Name="ShowScreenOne" Content="Show Screen One"/>
            <Button x:Name="ShowScreenTwo" Content="Show Screen Two"/>
            <Button x:Name="ShowSearchPersonScreen" Content="Show Search Person Screen"/>
            <Button x:Name="ShowEditPersonScreen" Content="Show Edit Person Screen" />
            <Button x:Name="ShowDetailsPersonScreen" Content="Show Details Person Screen"/>
        </StackPanel>
        <ContentControl Grid.Row="1"  x:Name="ActiveItem"/>


    </Grid>
</UserControl>
