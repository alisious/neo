<UserControl x:Class="Kseo2.Views.PersonAddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        
        <StackPanel Grid.Column="0" >
            <DockPanel>
            <TextBlock Text="Rodzaj adresu:" Width="150"/>
            <ComboBox x:Name="AddressType" Margin="0,0,40,0"
                      ItemsSource="{Binding AddressTypes}" 
                                        SelectedItem="{Binding CurrentAddressType}"
                                        SelectedValuePath="Name"
                                        IsEditable="False"/>
            </DockPanel>
            <ContentControl x:Name="Location"></ContentControl>
        </StackPanel>
    </Grid>
</UserControl>
